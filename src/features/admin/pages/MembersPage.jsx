import MemberListPage from './MemberListPage'

const MembersPage = () => (
  <MemberListPage
    title="Members"
    subtitle="Search, filter by type and payment status, open a record, and export CSV."
  />
)

export default MembersPage
